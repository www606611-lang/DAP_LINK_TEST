# 项目协作约定

## 构建约定

- 本项目默认只使用 GCC 构建：
```powershell
cmake --build build-gcc --target dap_link_test
```

- 不要默认运行 `build-ticlang`。
- 只有用户明确要求验证 TIClang 时，才运行：
```powershell
cmake --build build-ticlang --target dap_link_test
```

## 控制环边界

- 速度环和位置环是已经调好的基础能力，默认不要改它们的参数、输出限制、采样逻辑和核心 API。
- `encoder_speed_control.c/.h`、`encoder_position_control.c/.h` 只有在用户明确要求调整速度环或位置环时才能修改。
- 新模块可以串速度环使用，但只能通过已有 API 调用，例如设置目标速度、启动、停止、读取状态。
- 循迹、角度环等外层控制如果需要补偿左右电机差异，应优先在外层控制文件里做，不要改坏速度环和位置环基座。

## 循迹模块约定

- 八路灰度模块使用 I2C1，当前项目引脚为 PA16/PA17。
- 灰度读取已经调通后，不要随意改 `line_sensor_i2c.c/.h` 的底层 I2C 读写逻辑。
- 循迹 PID 可以串速度环：外层根据灰度误差计算左右目标速度，再调用速度环 API。
- 右轮机械差异明显、死区大，循迹调参时允许在 `line_tracking_control.c` 里做右轮单独补偿。
- 右轮补偿要保守小步调，优先观察是否起转、是否过冲、是否导致左右明显不平衡。
- LCD 可以显示八路原始状态、有效数量、循迹误差和开关状态，但不要加入过多串口或屏幕调试信息，避免影响实时性。

## 调试习惯

- 用户希望先分析现象和差异，再小步修改，不要盲目连续大改。
- 每次修改前说明要动哪些文件、为什么动；修改后说明是否影响速度环、位置环、IMU、灰度读取。
- 每次代码改动后优先跑 `build-gcc` 验证。
- 遇到电机、IMU、LCD、复位等耦合问题时，要同时考虑软件控制、供电纹波、死区、方向映射、采样周期和硬件接触问题。
- 已经验证稳定的功能要当作基座保护，新增功能尽量以独立模块和清晰 API 接入。

## 当前项目基座

- 电机速度环已完成并作为后续控制内环使用。
- 电机位置环已完成，位置环本质是位置外环加速度内环的串级控制。
- 角度 yaw 控制已完成基础 API，可通过 IMU yaw 角控制左右轮差速转向。
- 八路灰度循迹模块已接入，后续重点是在 `line_tracking_control.c` 调循迹 PID 和左右轮补偿。
