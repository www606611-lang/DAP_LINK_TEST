# Legacy Motor Bring-up

This directory preserves the original supervised open-loop motor workflow for
historical reference. It is not compiled into `car_control`.

Production motor commands use `bsp/board_wheel_drive`, and the validated speed,
position, Yaw, Heading, and line controllers remain the active test paths.
